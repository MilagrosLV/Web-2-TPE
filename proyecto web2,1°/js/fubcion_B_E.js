function editarLibro(id) {
    window.location = "editar.php?id=" + id;
}

function borrarLibro(id) {
    // Enviar una solicitud al servidor para borrar el libro con el ID correspondiente
    // Puedes usar AJAX o redirigir a una página PHP que maneje el borrado
    // Por ejemplo, "borrar.php?id=" + id
}
